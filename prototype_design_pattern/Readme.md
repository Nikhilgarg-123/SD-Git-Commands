This design pattern says then instead of creating objects where their is heavy constructor.

Use the copy/Clone of that First Object.

So here we use Cloneable Interface of JAVA. 

It Works like we have created a Backup of object and restore it when needed.
